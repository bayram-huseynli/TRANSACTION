server:
  port: 6969
spring:
  datasource:
    url: jdbc:mysql://localhost:1313/TR?createDatabaseIfNotExist=true
    username: root
    password: password
  jpa:
    hibernate.ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    generate-ddl: true
    show-sql: true
    open-in-view: false
